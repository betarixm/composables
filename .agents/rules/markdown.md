# Markdown Rules

Markdown files MUST pass the repository's configured `markdownlint` checks.

Follow the existing Markdown style and configuration. Do not suppress lint rules solely for convenience.
